#!/bin/sh
# Cria o primeiro admin (#15) em produção, onde não existe `mix`:
#   ADMIN_EMAIL=... ADMIN_SENHA=... bin/criar_admin
set -eu

cd -P -- "$(dirname -- "$0")"
exec ./unlokao eval Unlokao.Release.criar_admin
